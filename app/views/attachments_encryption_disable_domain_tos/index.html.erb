<h2>添付ファイル暗号化無効送信先ドメイン</h2>

<table class="table">
  <thead>
    <tr>
      <th>Domain</th>
      <th colspan="2"></th>
    </tr>
  </thead>

  <tbody>
    <% @attachments_encryption_disable_domain_tos.each do |attachments_encryption_disable_domain_to| %>
      <tr>
        <td><%= attachments_encryption_disable_domain_to.domain %></td>
        <td><%= link_to 'Edit', edit_attachments_encryption_disable_domain_to_path(attachments_encryption_disable_domain_to), class: 'btn' %></td>
        <td><%= link_to 'Destroy', attachments_encryption_disable_domain_to, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-error' %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New', new_attachments_encryption_disable_domain_to_path, class: 'btn' %>
