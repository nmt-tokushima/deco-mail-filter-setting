<%= form_with(model: api_access_allowed_ip, local: true, class: 'form-horizontal') do |form| %>
  <% if api_access_allowed_ip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(api_access_allowed_ip.errors.count, "error") %> prohibited this api_access_allowed_ip from being saved:</h2>

      <ul>
      <% api_access_allowed_ip.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <div class="col-3">
      <%= form.label :ip, class: 'form-label' %>
    </div>
    <div class="col-9">
      <%= form.text_field :ip, class: 'form-input' %>
    </div>
  </div>

  <div class="actions">
    <%= form.submit class: 'btn btn-primary' %>
  </div>
<% end %>
