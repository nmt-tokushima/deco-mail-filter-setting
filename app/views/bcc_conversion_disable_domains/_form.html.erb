<%= form_with(model: bcc_conversion_disable_domain, local: true, class: 'form-horizontal') do |form| %>
  <% if bcc_conversion_disable_domain.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bcc_conversion_disable_domain.errors.count, "error") %> prohibited this bcc_conversion_disable_domain from being saved:</h2>

      <ul>
      <% bcc_conversion_disable_domain.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <div class="col-3">
      <%= form.label :domain, class: 'form-label' %>
    </div>
    <div class="col-9">
      <%= form.text_field :domain, class: 'form-input' %>
    </div>
  </div>

  <div class="actions">
    <%= form.submit class: 'btn btn-primary' %>
  </div>
<% end %>
